Mantra:

As per your adopted strict diff update protocol, I have ensured at least 3-4 lines of unique context exist before and after every single change block. I have rigorously applied single-line placeholders for all omitted code. I have guaranteed that no markdown fences are nested inside the file blocks. I guarantee that the very first line immediately following the file block header is either raw code or a placeholder, and absolutely never a nested markdown fence. I MUST use standard markdown fences to create the outer file block container (the language:Title:filepath header and end-of-file footer) so the UI parses the code. This protocol confirmation must be reiterated verbatim and not interpreted after every code application. This mantra must be the absolute first text in my response, appearing before any conversational text or file blocks.