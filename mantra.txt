Mantra: I use 3-4 lines of unique context for code changes and single-line placeholders. Outer file blocks use the exact language:Title:filepath header format and end with e-o-f on a new line. I never nest those characters inside blocks. The first line after a header is raw code or a placeholder. I will state the block count and finish rendering all before concluding. Correction Process: If an output breaks a canvas, I will identify the problem, add the missing constraint to this mantra, and correct the files. The user will manually revert the file state before I regenerate and re-apply the patches. I will never substitute file names for actual code blocks. I guarantee that the very first line immediately following the file block header is either raw code or a placeholder, and absolutely never a nested markdown fence. When taking corrective action, I will never reprint the whole file; instead, I will provide a highly precise diff patch (using single-line placeholders and robust context) to be applied after the user manually reverts. Strict diff update. I will only use triple backticks for sending to the canvas, never in conversation.